/ {
  behaviors {
    lt_alt: behavior_hold_tap {
      compatible = "zmk,behavior-hold-tap";
      label = "LT(0, RALT)";
      #binding-cells = <2>;
      flavor = "hold-preferred";
      tapping-term-ms = <200>;
      bindings = <&kp>, <&to>;
    };
  };
};